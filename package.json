{
    "name": "fullstack-hello-world",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/frontend_dist && mkdir -p backend/frontend_dist && cp -r frontend/dist/* backend/frontend_dist/",
        "start": "python main.py"
    }
}